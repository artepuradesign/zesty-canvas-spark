
{
    "name": "apipainel/backend",
    "description": "Backend API para sistema de painéis",
    "type": "project",
    "require": {
        "php": ">=7.4",
        "firebase/php-jwt": "^6.0",
        "phpmailer/phpmailer": "^6.6",
        "ramsey/uuid": "^4.0",
        "monolog/monolog": "^2.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "migrate": "php src/migrations/migrate.php"
    },
    "config": {
        "optimize-autoloader": true
    }
}
