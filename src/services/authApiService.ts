
// Re-export from the refactored auth service for backward compatibility
export { authApiService } from './auth';
export type { RegisterData, LoginData, ApiUser, AuthApiResponse } from '@/types/auth';
